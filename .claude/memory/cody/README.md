# memory/cody/

Cody's memory files. Every file here MUST have `user: cody` in frontmatter.

This directory starts empty at scaffold time. Cody (they/them, GitHub `@karmuno`) populates from their machine using the "Claude & Cody" persona (`role-manifests/claude-and-cody.yaml`).

Pronoun discipline: they/them in any prose references to Cody.

Loaded automatically by `skills/load-memory/SKILL.md` when user-detection resolves to cody (per detection-layer rules in `docs/SSOT_System_Design_2026-04-28_v01_I.md` §4.1).
