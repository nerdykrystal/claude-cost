# rules/

Rule packs (governance documents that apply across the codebase).

Phase B migration: existing rules from `repos/.claude/rules/` land here.

Spec Genius Batch 3 may extend this with sandbox-rules and test-tautology-bans referenced by /asae rule-pack lookup (those primarily live in `mm-d2r-code-plan-stack/docs/`, but cross-referenced rule docs may land here).
