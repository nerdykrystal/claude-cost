# commands/

Custom slash commands.

Phase B migration: existing commands from `repos/.claude/commands/` land here. Currently expected: `remember.md`.

Spec Genius Batch 3 may add additional commands as they author the 6 new sub-skills (some of those may register slash command entry points here).
