# .repo-manifest.yaml — Martinez Methods Repo Taxonomy Manifest
# Read by: Claude instances at session start, health-check operations,
#          taxonomy compliance checks, propagation tooling
# Schema version: 1.0.0
#
# This file was generated by mm-claude-canonical propagation.
# Edit freely — propagation does NOT overwrite this file after creation.

schema_version: "1.0.0"

repo:
  # GitHub repo name (matches the slug in the URL)
  name: "claude-cost"

  # One of: canonical | application | research | portfolio |
  #         methodology | journal | workspace | operational
  type: "application"

  # One-sentence purpose statement. Claude reads this to orient.
  purpose: "Client-side TypeScript cost-estimator for D2R multi-model AI pipelines — core engine + web app + CLI. Estimate, compare, and analyze pipeline costs with ASAE-retry envelopes; no backend, no API keys, no telemetry."

  # GitHub org or owner: Martinez-Methods | nerdykrystal
  org: "nerdykrystal"

lifecycle:
  # One of: active | maintenance | archived | deprecated
  state: active

  # ISO 8601 date of repo creation
  created: "2026-06-15"

canonical:
  # Propagation tier: full | skills | none
  tier: "full"

asae:
  # Derived from repo type — see plan for defaults per type
  audit_threshold: "standard-2"
  going_public: false
